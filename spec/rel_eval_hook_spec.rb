require "spec_helper"

describe RelEvalHook do
  it "has a version number" do
    expect(RelEvalHook::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
